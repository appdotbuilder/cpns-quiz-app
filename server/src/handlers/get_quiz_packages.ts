import { type QuizPackage } from '../schema';

export async function getQuizPackages(): Promise<QuizPackage[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all active quiz packages from the database.
    // Should return only active packages for regular users, all packages for admins.
    return Promise.resolve([]);
}